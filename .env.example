# Copy this file to .env and fill in your secrets. Do NOT commit your .env with real secrets.
OLLAMA_CLOUD_API_KEY=your_ollama_cloud_api_key_here
# Optional: if you need to run docker with sudo via scripts (not recommended), set SUDO_PASSWORD=...
SUDO_PASSWORD=
